defaults:
  - optional dataset: null
  - optional model: null
  - _self_

runtime:
  seed: 42
  device: auto
  output_dir: outputs/${model.name}/

hydra:
  run:
    dir: ${runtime.output_dir}/${model.name}/

